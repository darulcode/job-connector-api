stages:
  - deploy

deploy:
  stage: deploy
  tags:
    - cq
  script:
    - ssh -i $SSH_PRIVATE_KEY $SSH_USER@$SSH_HOST "
      cd $PROJECT_DIR &&
      git pull origin master &&
      docker-compose up -d
      "
variables:
  PROJECT_DIR: "enigma-job-connector-api"
